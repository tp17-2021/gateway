version: "3.8"

services:
  synchronization-service:
    build:
      target: test
    restart: "no"
    depends_on:
      - web

  web:
    depends_on:
      - voting-process-manager
      - voting-service
      - vote-db


